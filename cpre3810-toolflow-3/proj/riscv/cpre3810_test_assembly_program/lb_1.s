.data

.text
.globl main

main:

    # Start Test

    addi x1, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x2, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x3, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x4, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x5, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x6, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x7, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x8, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x9, x0, 0     # verify that one can clear registers and 0+0 works in the ALU   
    addi x10, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x11, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x12, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x13, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x14, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x15, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x16, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x17, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x18, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x19, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x20, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x21, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x22, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x23, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x24, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x25, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x26, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x27, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x28, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x29, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x30, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   
    addi x31, x0, 0    # verify that one can clear registers and 0+0 works in the ALU   

 # end:
    wfi               
  #  j end             